#include <stdio.h>
/**
 * main - prints output using put without printf
 *
 * Return: 0 if exited properly,non-zero otherwise
 *
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
